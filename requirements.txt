# Edge-Radar — Python Dependencies

# === Core ===
python-dotenv==1.0.0
requests==2.31.0
cryptography==44.0.0              # RSA-PSS signing for Kalshi API auth
# aiohttp==3.9.1                  # planned — async HTTP
# pydantic==2.5.0                 # planned — data validation
# python-dateutil==2.8.2          # planned — date parsing

# === Trading APIs ===
# alpaca-trade-api==3.2.0       # planned — Stocks/options via Alpaca
# coinbase-advanced-py==1.0.0   # planned — Coinbase Advanced Trade

# === Prediction Markets ===
# py-clob-client==0.11.0        # planned — Polymarket CLOB
# kalshi-python==1.0.0          # planned — Kalshi (install from their repo)

# === Data & Analysis ===
scipy==1.11.4                     # Normal CDF for edge probability
# pandas==2.1.4                   # planned — dataframes for backtesting
# numpy==1.26.2                   # planned — numerical computing
# scikit-learn==1.3.2             # planned — ML models
# statsmodels==0.14.1             # planned — statistical models

# === Optimization (DFS Lineups) ===
# pulp==2.7.0                     # planned — Linear programming for DFS optimizer
# ortools==9.8.3296               # planned — Google OR-Tools (alternative)

# === Technical Analysis ===
# ta==0.11.0                      # planned — Technical indicators
# TA-Lib                          # Install separately: pip install TA-Lib (requires C library)

# === Database ===
# sqlalchemy==2.0.23              # planned — ORM
# alembic==1.13.0                 # planned — database migrations
# psycopg2-binary==2.9.9         # planned — PostgreSQL

# === Scheduling ===
# apscheduler==3.10.4             # planned — cron-style job scheduler

# === Notifications ===
agentmail==0.4.5                  # Daily edge report email delivery
# slack-sdk==3.26.1               # planned — Slack alerts

# === Web Scraping (for data sources without APIs) ===
# playwright==1.40.0              # planned — browser automation
# beautifulsoup4==4.12.2          # planned — HTML parsing
# lxml==4.9.3                     # planned — XML/HTML parsing

# === Odds & Stats APIs ===
# No official SDK — use requests directly with API keys

# === Web Dashboard ===
streamlit>=1.33.0                  # Streamlit web UI (uses @st.dialog, st.fragment)

# === Utilities ===
rich==13.7.0                      # Terminal tables & formatted output
# loguru==0.7.2                   # planned — structured logging
# tqdm==4.66.1                    # planned — progress bars for backtests

# === Testing ===
pytest==7.4.3
pytest-asyncio==0.23.8
pytest-mock==3.12.0

# === Dev Tools ===
black==23.12.0
flake8==6.1.0
mypy==1.7.1
pre-commit==3.6.0
